# --- OS & editors ---
.DS_Store
Thumbs.db
*~
*.swp
*.swo
.idea/
*.iml

# --- Local secrets (keep .env.example tracked) ---
.env
.env.local
.env.*.local
!.env.example

# --- Cursor ---
.cursor/

# --- Java / JVM (repo-wide; prefer backend/target/ for build output) ---
*.class
hs_err_pid*
replay_pid*

# Logs anywhere in the repo
*.log
logs/

# Archives (build artifacts; do not use blanket *.jar — it hides Maven Wrapper jars)
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# Legacy / tooling
*.ctxt
.mtj.tmp/

# Maven / Spring build outputs (monorepo)
**/target/
graphify-out/
.venv/